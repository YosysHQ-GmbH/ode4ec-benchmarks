Verilog arithmetic examples

Some toy examples for early exploration of how to interface with timing providers when dealing with word-level RTLIL designs.

Run each example's associated .ys file (e.g. `yosys 01_simple_adder.ys`) to generate RTLIL and Verilog netlist exports from before and after the alumacc step in a generic synthesis flow.

